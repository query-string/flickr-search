= form_tag search_index_path, method: :get, enforce_utf8: false
  = text_field_tag :query, params[:query], placeholder: 'Photo name, tags or description', type: :search
  = submit_tag 'Search', name: nil
